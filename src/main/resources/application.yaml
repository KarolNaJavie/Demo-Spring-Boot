spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coding
    username: root
    password: ROOT
    driverClassName: com.mysql.cj.jdbc.Driver
  application:
    name: Projekt X
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.hibernate.orm.jdbc.bind: TRACE